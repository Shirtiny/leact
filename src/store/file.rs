#[derive(Clone, Copy)]
pub struct FileState {
    // pub file: Option<web_sys::File>,
}
// pub fn use_file_provider() -> FileState {
//     let state = use_context_provider(|| FileState { file: None });
// }
